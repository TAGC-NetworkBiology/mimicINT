
This image contains:

 - Python 3.5
      With pip and the following Python packages:
     - pandas
     - scipy
     - click (no longer necessary with current script 
              versions but let for compatibility with
              previous versions of the pipeline)
